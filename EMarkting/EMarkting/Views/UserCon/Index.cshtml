@using PagedList;
@using PagedList.Mvc;

@model IPagedList<EMarkting.Models.Category>
 
<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>View  Category</title>
    <link href="~/Content/paginition.css" rel="stylesheet" />
    <link href="~/Content/bootstrap.min.css" rel="stylesheet" />
    <link href="~/Content/bootstrap.css" rel="stylesheet" />

    <script>
        alert("admin  email:  admin ,, pas:admin  ");
        alert("user  email:  eslam ,, pas:eslam  ");
    </script>
</head>
<body>
    <div class="container">
        @*style="height:1150px; "*@

        @*<div class="row">
            <div class="col-lg-12" style="height:100px; border:1px solid solid; ">
                <img src="~/Content/img/Modern-e-Commerce-Web-Design-Trends.-Part-2.gif" style="width:100%; height:100%" />
            </div>
        </div>*@



        <div class="row" style="margin-bottom:40px">
            @if (Model != null)
        {
            foreach (var item in Model)
            {
                <div class="col-lg-4 col-md-4" style="height:200px;margin-top:100px">
                    <img src="~/ImgUpload/@item.Image" style=" height:100%;width:100% ;border-radius:50px 50px 50px 50px" />
                    <h4 style="color:#808080; font-family:'Arial Rounded MT';text-align:center">@Html.ActionLink(@item.Name, "Ads", new { id = @item.Id })  </h4>
                </div>

        }
    }
        </div>
    </div>

    @Html.PagedListPager(Model, Page => Url.Action("ViewCategory", new
    { Page }), new PagedListRenderOptions()
    {
        Display = PagedListDisplayMode.IfNeeded,
        DisplayPageCountAndCurrentLocation = true
    })

</body>
</html>

